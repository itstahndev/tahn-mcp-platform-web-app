// Clerk auth helpers — to be configured in Phase 05
// This file will export auth utilities once @clerk/nextjs is installed.

export function getAuthToken(): string | null {
  // Placeholder — will use Clerk's auth() in server components
  return null;
}
